---
"@inkandswitch/patchwork-providers-solid": patch
---

Allow Solid provider helpers to accept lazily resolved elements, so components can pass accessors that are evaluated on mount instead of requiring an `HTMLElement` up front.
