# Borrowed Bits
To break the circular dependencies in `@patchwork/sdk` with other packages, I'm just copying anything from those packages I need into here.

We should evaluate more closely what we should do about these things once the build is working again.
