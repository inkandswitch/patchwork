diff --git a/dist/subduction/SubductionConnections.js b/dist/subduction/SubductionConnections.js
index aa1028cde47b5f0d36d7a96d855d1eda5f5ee884..aa5bbda365180fb67cab1948ef6da1d246263537 100644
--- a/dist/subduction/SubductionConnections.js
+++ b/dist/subduction/SubductionConnections.js
@@ -31,25 +31,34 @@ export class SubductionConnections {
     async manageConnection(url) {
         const serviceName = new URL(url).host;
         let backoff = RECONNECT_BASE_MS;
+        let connectionAttempt = 0;
         while (!this.#isShutdown) {
+            connectionAttempt++;
             this.#setConnectionState(url, "connecting");
-            this.#log(`connecting to ${url}...`);
+            const startedAt = performance.now();
+            this.#log(`connecting to ${url} (attempt=${connectionAttempt}, generation=${this.#generation})...`);
             try {
                 const transport = await WebSocketTransport.connect(url);
+                const openMs = Math.round(performance.now() - startedAt);
+                this.#log(`websocket open to ${url} after ${openMs}ms`);
                 if (this.#isShutdown) {
                     transport.disconnect();
                     break;
                 }
                 const subduction = await this.#subduction;
+                const handshakeStartedAt = performance.now();
                 await subduction.connectTransport(transport, serviceName);
                 this.#setConnectionState(url, "running");
-                this.#log(`connected to ${url}`);
+                const handshakeMs = Math.round(performance.now() - handshakeStartedAt);
+                this.#log(`connected to ${url} (service=${serviceName}, handshake=${handshakeMs}ms, generation=${this.#generation})`);
                 backoff = RECONNECT_BASE_MS;
                 await transport.closed();
-                this.#log(`disconnected from ${url}`);
+                const lifetimeMs = Math.round(performance.now() - startedAt);
+                this.#log(`disconnected from ${url} after ${lifetimeMs}ms (generation=${this.#generation})`);
             }
             catch (e) {
-                console.warn(`[subduction] connection to ${url} failed:`, e);
+                const failedMs = Math.round(performance.now() - startedAt);
+                console.warn(`[subduction] connection to ${url} failed after ${failedMs}ms:`, e);
             }
             if (this.#isShutdown)
                 break;
diff --git a/dist/subduction/SyncScheduler.js b/dist/subduction/SyncScheduler.js
index e424bc413b4f968233acbe6112ac315789612c2e..750a3477327c68dd92381caf589a9e3f3e565327 100644
--- a/dist/subduction/SyncScheduler.js
+++ b/dist/subduction/SyncScheduler.js
@@ -3,6 +3,30 @@ import { toDocumentId } from "./helpers.js";
 const HEAL_INITIAL_DELAY_MS = 2_000;
 const HEAL_MAX_DELAY_MS = 60_000;
 const HEAL_MAX_ATTEMPTS = 10;
+const SUBDUCTION_SYNC_TIMEOUT_MS = 30000n;
+let syncAttemptCounter = 0;
+function nextSyncAttemptId(prefix) {
+    syncAttemptCounter = (syncAttemptCounter + 1) % Number.MAX_SAFE_INTEGER;
+    return `${prefix}-${syncAttemptCounter.toString(36)}`;
+}
+function statValue(value) {
+    return typeof value === "bigint" ? value.toString() : value;
+}
+function summarizeSyncResults(results) {
+    return results.map(result => ({
+        peerId: result.peerId?.toString?.() ?? result.peerId,
+        success: result.success,
+        transportErrors: result.transportErrors?.map?.(error => String(error)) ?? [],
+        stats: result.stats
+            ? {
+                commitsSent: statValue(result.stats.commitsSent),
+                commitsReceived: statValue(result.stats.commitsReceived),
+                fragmentsSent: statValue(result.stats.fragmentsSent),
+                fragmentsReceived: statValue(result.stats.fragmentsReceived),
+            }
+            : undefined,
+    }));
+}
 /**
  * Manages self-healing retry logic for failed syncs.
  *
@@ -70,10 +94,17 @@ export class SyncScheduler {
         this.#healAttempts.set(key, (this.#healAttempts.get(key) ?? 0) + 1);
         this.#log(`executing heal sync for ${key.slice(0, 8)}...`);
         try {
+            const attemptId = nextSyncAttemptId(`heal-${key.slice(0, 8)}`);
+            const startedAt = performance.now();
+            this.#log(`heal sync ${attemptId} start timeout=${SUBDUCTION_SYNC_TIMEOUT_MS}ms`);
             const subduction = await this.#subduction;
-            const peerResultMap = await subduction.syncWithAllPeers(sedimentreeId, true);
+            const peerResultMap = await subduction.syncWithAllPeers(sedimentreeId, true, SUBDUCTION_SYNC_TIMEOUT_MS);
             const results = peerResultMap.entries();
+            const durationMs = Math.round(performance.now() - startedAt);
             const anyFailed = results.some(r => !r.success || (r.transportErrors?.length ?? 0) > 0);
+            this.#log(`heal sync ${attemptId} done after ${durationMs}ms: ` +
+                `${results.length} peer(s), anyFailed=${anyFailed}, ` +
+                `results=${JSON.stringify(summarizeSyncResults(results))}`);
             // If new data was received during the heal, load it into the handle
             // immediately — otherwise the recovered commits/fragments would sit
             // in subduction storage until some other event surfaced them.
diff --git a/dist/subduction/source.js b/dist/subduction/source.js
index bb60ed598bc912f0dc680d95ff1997db6e82079d..fbb6222ad42097882926f08a524d8022817291e2 100644
--- a/dist/subduction/source.js
+++ b/dist/subduction/source.js
@@ -9,6 +9,30 @@ import { SubductionConnections } from "./SubductionConnections.js";
 import { SyncScheduler } from "./SyncScheduler.js";
 import { AdapterConnections } from "./AdapterConnections.js";
 const RECENTLY_SAVED_CACHE_SIZE = 256;
+const SUBDUCTION_SYNC_TIMEOUT_MS = 30000n;
+let syncAttemptCounter = 0;
+function nextSyncAttemptId(prefix) {
+    syncAttemptCounter = (syncAttemptCounter + 1) % Number.MAX_SAFE_INTEGER;
+    return `${prefix}-${syncAttemptCounter.toString(36)}`;
+}
+function statValue(value) {
+    return typeof value === "bigint" ? value.toString() : value;
+}
+function summarizeSyncResults(results) {
+    return results.map(result => ({
+        peerId: result.peerId?.toString?.() ?? result.peerId,
+        success: result.success,
+        transportErrors: result.transportErrors?.map?.(error => String(error)) ?? [],
+        stats: result.stats
+            ? {
+                commitsSent: statValue(result.stats.commitsSent),
+                commitsReceived: statValue(result.stats.commitsReceived),
+                fragmentsSent: statValue(result.stats.fragmentsSent),
+                fragmentsReceived: statValue(result.stats.fragmentsReceived),
+            }
+            : undefined,
+    }));
+}
 export class SubductionSource {
     #subduction;
     #storage;
@@ -258,10 +282,17 @@ export class SubductionSource {
             entry.flushSave.flush();
             await entry.saveSettled;
             this.#log(`doSync ${sid} (state=${entry.syncState})`);
-            const peerResultMap = await subduction.syncWithAllPeers(sedimentreeId, true);
+            const attemptId = nextSyncAttemptId(`sync-${sid}`);
+            const startedAt = performance.now();
+            this.#log(`doSync ${sid} ${attemptId} start ` +
+                `(state=${entry.syncState}, timeout=${SUBDUCTION_SYNC_TIMEOUT_MS}ms, generation=${entry.lastSyncGeneration})`);
+            const peerResultMap = await subduction.syncWithAllPeers(sedimentreeId, true, SUBDUCTION_SYNC_TIMEOUT_MS);
             const results = peerResultMap.entries();
+            const durationMs = Math.round(performance.now() - startedAt);
             const anySuccess = results.some(r => r.success);
             this.#log(`doSync ${sid}: ${results.length} peer(s), success=${anySuccess}`);
+            this.#log(`doSync ${sid} ${attemptId} done after ${durationMs}ms: ` +
+                `results=${JSON.stringify(summarizeSyncResults(results))}`);
             // Check if any data was received from peers
             const dataReceived = results.some(r => {
                 const s = r.stats;
