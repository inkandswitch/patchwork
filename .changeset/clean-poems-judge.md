---
"@inkandswitch/patchwork-elements": patch
---

Declare `patchwork-view` in `HTMLElementTagNameMap`, so `document.querySelector("patchwork-view")` is typed. Move component-loading chatter to a `patchwork:elements:patchwork-view` debug logger.
