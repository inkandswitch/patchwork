export type {
  PathSegment,
  RefOptions,
  DynamicSegment,
  ChangeEvent,
  ChangeCallback,
  PathBuilder,
} from "./types";

export { Ref, ref } from "./ref";

export { splice, updateText, isCursor, at, isDynamic } from "./utils";
